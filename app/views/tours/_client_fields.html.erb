<tr class="nested-fields">
  <td>
    <%= link_to_remove_association "remove client", f, class: 'btn btn-primary btn-xs' %>
  </td>

  <td>
    <%= f.input_field :id, :as => :select, class: 'select2', selected: (@tour.new_record? or @tour.clients.blank?) ? ['Select user'] : f.object.id,
                      collection: (@tour.new_record? or @tour.clients.blank?)  ? ['Select client'] : { :"#{f.object.fio}" => f.object.id },
                      style: 'width: 250px;' %>
  </td>

</tr>